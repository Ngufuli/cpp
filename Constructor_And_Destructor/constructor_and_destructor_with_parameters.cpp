/*	
	CONSTRUCTOR AND DESTRUCTOR WITH PARAMETERS/PARAMETERS IN BASE CLASS AND DERIVED CLASS
	
	In this case if base class constructor takes some parameters, it is mandatory for the derived class
	to have parameterized constructor which contains parameters from base class constructor along with it's own
	parameters.
*/
